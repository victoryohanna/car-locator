{
  "name": "wootlab-test",
  "version": "1.0.0",
  "description": "This is a simple application that register cars and update their locations for a specific date and time and show the positions of the cars in a map.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "cd client && npm start",
    "server": "cd server && npm start",
    "start": "concurrently \"npm run server\" \"npm run client\""
  },
  "author": "Victor Yohanna",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.4.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
